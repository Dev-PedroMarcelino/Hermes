{
  "name": "hermes-root",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "engine": "npm start --prefix engine",
    "engine:dev": "npm run dev --prefix engine",
    "worker": "npm run worker --prefix engine",
    "dashboard": "npm run dev --prefix dashboard",
    "install:all": "npm install && npm install --prefix engine && npm install --prefix dashboard",
    "test": "npm test --prefix engine"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  }
}
